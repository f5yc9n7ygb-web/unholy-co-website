name = "unholy-release-reservations-cron"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
# Temporarily paused — re-enable to "*/10 * * * *" after Pages deploy rolls out
# the route fix that decouples this cron from the abandoned-cart email flow.
crons = []

[vars]
SITE_URL = "https://theunholy.co"
# CRON_SECRET must be set via: wrangler secret put CRON_SECRET
